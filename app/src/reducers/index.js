import { combineReducers } from "redux";
import emailClientRed from "./emailClientRed";
const rootReducer = combineReducers({
  /**COMPONENTS EXM: oneComponent, secondComponent*/
  emailClientRed
});

export default rootReducer;
